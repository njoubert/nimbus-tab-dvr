<!--
House style: docs/WRITING_STYLE.md. Terse and formal, one sentence per bullet, no em dashes.
Write each paragraph as ONE unwrapped line; GitHub renders a newline here as a line break.
Omit a section that does not apply rather than writing "n/a".
-->

# Summary

<!--
What this branch changes and why, in two or three sentences, each on one line.
Then address the reviewer: "Reviewers: start with <file>, which ...".

Draw the shape where there is one: a boundary that moved, the path a request takes, a state
machine, a sequence between two processes. GitHub renders a mermaid fence natively, so the
picture belongs in the body rather than in ASCII art, and a fence is exempt from the
one-unwrapped-line rule. Say in one sentence above it what it shows.
-->

<!-- A branch carrying a report links its published artifact page here. -->

# Design decisions

<!--
The choices this branch makes and the alternatives it rejects. This section carries the most
weight, so write it last, when the shape is settled. One bullet per decision: what was chosen,
what it was chosen over, and what it costs. A decision with no rejected alternative is usually
a description; cut it.
-->

- **The decision:** what it was chosen over, and what it costs.

# Verification

<!--
What was run and what it showed: ./build.sh check, ./build.sh test, a manual walk-through.
Pushing runs no suites, so this section is the only account of what was verified. Name what
ran, and name what none of it can see.
-->

- **Ran:**
- **Not covered:**
